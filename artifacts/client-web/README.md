# client-web

React Router frontend for TUtorMatch.

Architecture, folder layout, and conventions: [docs/technical/client-web.md](../../docs/technical/client-web.md).

Run locally with `make up` from the repo root, or `make client-web-pnpm ARGS="run dev"` for the dev server only.
