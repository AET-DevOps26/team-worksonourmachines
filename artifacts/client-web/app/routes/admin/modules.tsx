import { PlaceholderPage } from '~/components/pages';

export default function AdminModulesRoute() {
    return (
        <PlaceholderPage
            description="Create and manage modules, topics, and difficulty hints."
            title="Manage modules"
        />
    );
}
