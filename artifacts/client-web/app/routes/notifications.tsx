import { PlaceholderPage } from '~/components/pages';

// this might be moved later on
export default function NotificationsRoute() {
    return <PlaceholderPage description="Plan updates, messages, and other alerts." title="Notifications" />;
}
