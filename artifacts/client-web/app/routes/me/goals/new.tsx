import { PlaceholderPage } from '~/components/pages';

export default function NewLearningGoalRoute() {
    return (
        <PlaceholderPage
            description="Define module, topics, target date, budget, and location preferences."
            title="New learning goal"
        />
    );
}
