import { PlaceholderPage } from '~/components/pages';

export default function DiscoverRoute() {
    return (
        <PlaceholderPage
            description="Search and filter tutors by module, language, location, and budget."
            title="Discover tutors"
        />
    );
}
