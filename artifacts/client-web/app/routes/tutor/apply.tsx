import { PlaceholderPage } from '~/components/pages';

export default function TutorApplyRoute() {
    return (
        <PlaceholderPage
            description="Submit your tutor application with module coverage and certificates."
            title="Apply as tutor"
        />
    );
}
