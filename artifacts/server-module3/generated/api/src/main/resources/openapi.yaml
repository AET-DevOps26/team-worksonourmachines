openapi: 3.0.0
info:
  title: Module3 API
  version: v1
servers:
- description: Module3 API
  url: https://module3.example.com
paths:
  /v1/test:
    get:
      operationId: test
      parameters: []
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/test_200_response"
          description: The request has succeeded.
      x-accepts:
      - application/json
components:
  schemas:
    test_200_response:
      example:
        message: message
      properties:
        message:
          type: string
      required:
      - message
      type: object
