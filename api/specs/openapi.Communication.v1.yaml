openapi: 3.0.0
info:
  title: Communication API
  version: v1
tags: []
paths:
  /v1/test:
    get:
      operationId: test
      parameters: []
      responses:
        '200':
          description: The request has succeeded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                required:
                  - message
security:
  - KeycloakBearerAuth: []
components:
  securitySchemes:
    KeycloakBearerAuth:
      type: http
      scheme: Bearer
      description: Keycloak access token issued to the BFF client
servers:
  - url: https://communication.example.local
    description: Communication API
    variables: {}
