openapi: 3.0.0
info:
  title: AI API
  version: v1
tags: []
paths:
  /v1/plan:
    post:
      operationId: generatePlan
      summary: Generate study plan
      description: Fetches the learning goal and student from the Student API, tutors from the Marketplace API, then calls the LLM to generate three study-plan suggestions (cheapest, within_budget, best_quality). Does not persist anything.
      parameters: []
      responses:
        '200':
          description: The request has succeeded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Shared.Ai.GeneratePlanResponse'
        '401':
          description: Access is unauthorized.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Shared.Errors.ErrorBody'
        '404':
          description: The server cannot find the requested resource.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Shared.Errors.ErrorBody'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Shared.Ai.GeneratePlanRequest'
security:
  - KeycloakClientAuth:
      - openid
      - profile
      - email
      - roles
components:
  schemas:
    Shared.Ai.GeneratePlanRequest:
      type: object
      required:
        - learningGoalId
        - studentId
      properties:
        learningGoalId:
          type: string
        studentId:
          type: string
          description: UUID of the student for whom the plan is generated. Provided by the student service; the AI must not derive identity from the inbound token.
    Shared.Ai.GeneratePlanResponse:
      type: object
      required:
        - learningGoalId
        - suggestions
      properties:
        learningGoalId:
          type: string
        suggestions:
          type: array
          items:
            $ref: '#/components/schemas/Shared.Ai.PlanSuggestion'
    Shared.Ai.PlanMilestone:
      type: object
      required:
        - title
        - dueDate
        - topicId
        - tutorId
        - estimatedCost
      properties:
        title:
          type: string
        dueDate:
          type: string
          format: date-time
        topicId:
          type: string
        tutorId:
          type: string
        estimatedCost:
          type: integer
          format: int32
          description: Estimated cost in EUR (whole euros).
    Shared.Ai.PlanSuggestion:
      type: object
      required:
        - tier
        - description
        - totalEstimatedCost
        - proposedTutors
        - milestones
      properties:
        tier:
          $ref: '#/components/schemas/Shared.Ai.PlanTier'
        description:
          type: string
        totalEstimatedCost:
          type: integer
          format: int32
          description: Total estimated cost in EUR (whole euros).
        proposedTutors:
          type: array
          items:
            $ref: '#/components/schemas/Shared.Ai.ProposedTutor'
        milestones:
          type: array
          items:
            $ref: '#/components/schemas/Shared.Ai.PlanMilestone'
    Shared.Ai.PlanTier:
      type: string
      enum:
        - cheapest
        - within_budget
        - best_quality
    Shared.Ai.ProposedTutor:
      type: object
      required:
        - id
        - displayName
        - hourlyRate
      properties:
        id:
          type: string
        displayName:
          type: string
        hourlyRate:
          type: integer
          format: int32
          description: Hourly rate in EUR (whole euros).
    Shared.Errors.ErrorBody:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
        message:
          type: string
  securitySchemes:
    KeycloakClientAuth:
      type: oauth2
      flows:
        clientCredentials:
          tokenUrl: https://auth.tutormatch.localhost/realms/tutormatch/protocol/openid-connect/token
          scopes:
            openid: ''
            profile: ''
            email: ''
            roles: ''
      description: Keycloak OAuth2 client credentials auth for Scalar/API testing
servers:
  - url: https://ai.example.local
    description: AI API
    variables: {}
