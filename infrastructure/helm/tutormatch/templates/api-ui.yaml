apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-ui
  namespace: {{ .Values.namespace }}
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app: api-ui
  template:
    metadata:
      labels:
        app: api-ui
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: api-ui
          image: {{ include "tutormatch.image" (list . "api-ui" .Values.apiUi.imageDigest) | quote }}
          imagePullPolicy: {{ .Values.apiUi.imagePullPolicy | default "Always" }}
          ports:
            - containerPort: 8080
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
              add:
                - NET_BIND_SERVICE
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: caddy-config
              mountPath: /config/caddy
            - name: caddy-data
              mountPath: /data/caddy
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 2
            periodSeconds: 10
          resources:
            requests:
              cpu: 25m
              memory: 32Mi
            limits:
              cpu: 50m
              memory: 64Mi
      volumes:
        - name: tmp
          emptyDir: {}
        - name: caddy-config
          emptyDir: {}
        - name: caddy-data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: api-ui
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: api-ui
  ports:
    - port: 8080
      targetPort: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-ui
  namespace: {{ .Values.namespace }}
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - api.{{ .Values.ingress.host }}
      secretName: api-ui-tls
  rules:
    - host: api.{{ .Values.ingress.host }}
      http:
        paths:
          - path: /v1/students
            pathType: Prefix
            backend:
              service:
                name: server-student
                port:
                  number: {{ .Values.serverStudent.port }}
          - path: /v1/admin
            pathType: Prefix
            backend:
              service:
                name: server-marketplace
                port:
                  number: {{ .Values.serverMarketplace.port }}
          - path: /v1/modules
            pathType: Prefix
            backend:
              service:
                name: server-marketplace
                port:
                  number: {{ .Values.serverMarketplace.port }}
          - path: /v1/tutors
            pathType: Prefix
            backend:
              service:
                name: server-marketplace
                port:
                  number: {{ .Values.serverMarketplace.port }}
          - path: /v1/tutor-applications
            pathType: Prefix
            backend:
              service:
                name: server-marketplace
                port:
                  number: {{ .Values.serverMarketplace.port }}
          - path: /v1/conversations
            pathType: Prefix
            backend:
              service:
                name: server-communication
                port:
                  number: {{ .Values.serverCommunication.port }}
          - path: /v1/plan
            pathType: Prefix
            backend:
              service:
                name: ai
                port:
                  number: 8000
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api-ui
                port:
                  number: 8080
